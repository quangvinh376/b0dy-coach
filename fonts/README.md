# Font

App dùng **SVN-Switzer** (weight 500) làm font chữ chính. Thả hai file vào thư mục này:

- `SVN-Switzer-Medium.woff2`
- `SVN-Switzer-Medium.woff` (dự phòng)

Chưa có file thì app tự lùi về Switzer (Fontshare) rồi font hệ thống. Số và nhãn dùng JetBrains Mono (Google Fonts).
